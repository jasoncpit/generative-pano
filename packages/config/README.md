# packages/config

Shared configuration and environment variable names/constants for both apps.

Suggested usage:
- Centralize env var names and defaults here
- Re-export typed config helpers per app if needed


