services:
  api:
    build:
      context: .
      dockerfile: infra/Dockerfile.api
    environment:
      - CORS_ORIGIN=*
      - REPLICATE_API_TOKEN=${REPLICATE_API_TOKEN}
    ports:
      - "8000:8000"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthz"]
      interval: 10s
      timeout: 3s
      retries: 10

  web:
    build:
      context: .
      dockerfile: infra/Dockerfile.web
    environment:
      - NEXT_PUBLIC_API_BASE=http://localhost:8000/api/generate
    ports:
      - "3000:3000"
    depends_on:
      api:
        condition: service_healthy


